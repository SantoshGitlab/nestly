# Nestly QA Report — 2026-08-07

**Branch:** `main` @ `bcef432` · **Lead:** QA orchestration pass · **Apps:** customer-web, admin-web, provider-web

## VERDICT: NO-GO

Not because defects were found — because **the release-gating verification never ran**. Phase 3 (browser walkthrough) and Phase 4 (cross-service consistency) were blocked by a dependency outside my control. **Zero of the 587 inventoried features have been runtime-verified.** No feature in this report may be described as "working."

---

## 1. Phase status

| Phase | Scope | Status |
|---|---|---|
| 0 — Environment sanity | containers, 3 APIs, 3 frontends, ports, CORS | **GO** (after fixes) |
| 1 — Seed data coverage | row counts, FK orphans, distribution | **COMPLETE** |
| 2 — Feature inventory | 3 apps, static analysis | **COMPLETE** — 587 items |
| 3 — Browser walkthrough | execute all 587 items in Chrome | **BLOCKED — NOT RUN** |
| 4 — Cross-service consistency | booking visible across 3 apps | **BLOCKED — NOT RUN** |

**Blocker:** the claude-in-chrome extension is not connected (`list_connected_browsers` → `[]`). It requires manual connection + localhost permission by the user. Phases 3 and 4 are browser-mandated by design — the brief explicitly rules out curl, because CORS, localStorage, cookie, and hydration bugs surface only client-side.

---

## 2. Environment (Phase 0) — GO

All six services verified serving **from `main`**, returning real payloads, not just open ports.

| Check | Result |
|---|---|
| postgres | PASS — `nestly` DB, `SELECT 1` OK |
| redis | PASS — `PING` → `PONG` |
| consumer-api :5257 | PASS — `/health/ready` 200; `/geography/cities` returns real JSON |
| admin-api :5177 | PASS — `/health/ready` 200 |
| provider-api :5337 | PASS — `/health/ready` 200 |
| customer-web :3000 | PASS — 200, from `main` |
| admin-web :3001 | PASS — 200, from `main` |
| provider-web :3002 | PASS — 200, from `main` |
| CORS consumer-api | PASS — `["http://localhost:3000"]` |
| CORS admin-api | PASS — `["http://localhost:3001","http://localhost:3000"]` |
| CORS provider-api | PASS — `["http://localhost:3002","http://localhost:3000"]` |
| frontend API base URLs | PASS — 3000→5257, 3001→5177, 3002→5337 |
| **Chrome extension** | **FAIL — not connected. Blocks Phase 3/4.** |

**Critical finding:** before remediation, all three APIs and the customer-web frontend were serving from the **`goofy-ptolemy-842944` worktree**, not `main`. Any QA run in that state would have tested the wrong code with untraceable results.

Service logs: `/Users/mukesh/.claude-work/jobs/a91725d8/tmp/{api-consumer,api-admin,api-provider,web-customer,web-admin,web-provider}.log`

---

## 3. Consolidated bug list

### Fixes applied this pass

| # | Severity | Issue | Root cause | Fix | Files/objects changed |
|---|---|---|---|---|---|
| F1 | **Blocker** | 5 migrations unappliable | Demo seed piled 41 illegally-overlapping bookings on one provider, violating `ex_booking_provider_no_double_booking` | Redistributed 41 surplus rows onto 2 idle Active providers | `booking` table (41 rows) |
| F2 | **Blocker** | Schema 5 migrations behind `main` | Blocked by F1 | Applied all 5 | `__EFMigrationsHistory`; created `provider_location_ping`, `booking_tracking`; +15 notification templates (48→63) |
| F3 | **Major** | All 3 APIs + customer-web served from a stale worktree | Orphaned processes from prior sessions squatting ports | Killed 10 PIDs; restarted all 6 services from `main` | none (process state) |

**F1 verification:** 41 rows updated · overlapping pairs 0 (was 45) · total bookings 217 (unchanged) · unassigned 108 (unchanged) · no NULLs introduced · providers holding work 2→4. Backup: `booking-backup-pre-fix.sql`.

Pre-flight checks that made F1 safe to run: max partition group size = 3 (so the `CASE WHEN 2/WHEN 3` cannot yield NULL), **zero** partial overlaps (so distinct partitions cannot collide after reassignment), both targets `Active` with 0 existing bookings.

### Defects found by static analysis — NOT FIXED, NOT RUNTIME-CONFIRMED

Found by reading code, not by executing it. Each needs runtime confirmation in Phase 3.

| # | Sev | App | Defect | Location |
|---|---|---|---|---|
| S1 | **Major** | provider | Phase 16 statuses `EnRoute`/`Arrived`/`Withdrawn` missing from the jobs status filter, though badges render them — the tracking feature's own UI can't filter for it | `jobs/page.tsx:27-35` |
| S2 | **Major** | provider | 404 page's only escape hatch links to `/dashboard`, which does not exist in provider-web → another 404 | `not-found.tsx:19` |
| S3 | **Major** | provider | Same broken `/dashboard` link on the route error boundary | `error.tsx:65` |
| S4 | **Major** | provider | `refreshSession()` exported but never called — provider sessions cannot be renewed, they expire and eject to `/login` | `lib/auth-api.ts:43` |
| S5 | **Major** | customer | Quantity + add-ons chosen in the service-detail price calculator are **discarded**; "Book now" passes only `?serviceSlug=`, restarting at qty 1 | `PriceCalculator.tsx:24-37` vs `services/[slug]/page.tsx:127-132` |
| S6 | **Major** | customer | Detail-page quantity stepper has **no upper bound**; booking summary caps at 10 → a price quoted for qty 15 is unbookable | `PriceCalculator.tsx:92` |
| S7 | **Major** | admin | "Admin Users" nav gated on `settings.read` but its controls check `admin-users.write` → a role with `settings.read` alone lands on a page with every control hidden | `lib/permissions.ts:85` |
| S8 | Minor | customer | "Leave a review" rendered for **every** booking status; on non-completed bookings the target says it can't be reviewed — reads as a broken link | `bookings/[id]/page.tsx:416` |
| S9 | Minor | customer | Address form takes raw lat/long defaulting to `0`; `0,0` passes validation → address saveable at null island | `AddressForm.tsx:94-109` |
| S10 | Minor | admin | Reschedule form takes raw "Locality ID"/"Slot window ID" GUID text inputs, no picker — will always 400 without DB-sourced GUIDs | `bookings/[bookingId]/page.tsx:644-646` |
| S11 | Minor | customer | Error boundary only `console.error`s; no telemetry emitted | `app/error.tsx:24-28` |

### Environment-dependent, will read as failures in Phase 3

| # | App | Behavior | Cause |
|---|---|---|---|
| E1 | customer | Tracking map **always** renders "Live map unavailable" | `NEXT_PUBLIC_GOOGLE_MAPS_API_KEY` unset |
| E2 | admin | Booking-detail map renders grey placeholder | same |
| E3 | provider | Location pings fail **silently** — swallowed into exponential backoff, never surfaced in UI | verify in Network tab, not on screen |
| E4 | customer | Admin/Provider login tabs dead-end | admin-web/provider-web URLs unset in `.env.local` |

---

## 4. Seed data (Phase 1)

**FK integrity: clean.** All 88 constraints validated, zero orphans. 14 soft (unconstrained) uuid references hand-checked — zero orphans. Geo hierarchy resolves end-to-end.

| Entity | Rows | Flag |
|---|---|---|
| category | 11 | OK |
| service | 25 | OK (all active — no inactive variety) |
| provider | 27 | OK |
| customer | 36 | OK |
| booking | 217 | count OK, **badly skewed** |
| review | 73 | OK (no 1★/2★) |
| coupon | 4 | LOW (0 redemptions) |
| city | 2 | **effectively single-row** |
| zone | 2 | **effectively single-row** |
| pincode | 2 | **effectively single-row** |

**35 tables completely empty**, including: `banner`, `chat_message`, `cms_page`, `cms_faq`, `cms_media`, `coupon_redemption`, `device_token`, `provider_availability_window`, `provider_capacity`, `provider_kyc_document`, `provider_payout`, `recurring_booking_plan`, `referral`, `service_addon`, `service_city_price`, `subscription_plan`, `support_ticket`, `wallet_ledger`.

### Distribution defects that would corrupt a walkthrough

- **All 217 bookings belong to ONE customer** — 35 of 36 customers render empty everywhere.
- **108 bookings unassigned**; 23 of 27 providers still have zero work even after F1.
- **Only 2 of 25 services ever booked**; 9 of 11 categories have zero traffic.
- `provider_skill_mapping.service_id` **NULL on all 50 rows** → service-level matching has no data.
- `provider_service_area.pincode_id` **NULL on all 25 rows** → pincode serviceability untestable.
- `customer_address`: 30 of 32 rows have NULL `pincode_id` AND `locality_id`.
- `chat_thread` 41 rows / `chat_message` **0** → 41 clickable-but-blank conversations.
- **No 1★ or 2★ reviews** → rating filters, flag queue, moderation UI have no input.
- `EXPIRED10` coupon **is not actually expired** (merely inactive) → the expired-coupon case does not exist.

### Data bugs worth fixing regardless

- **Pincode `560001` (a real Bengaluru pincode) is parented to `E2E City`**, not Bengaluru. Any 560001→city lookup returns the fake test city.
- `booking_provider_assignment` has **1 row against 109 assigned bookings** — assignment audit trail effectively absent.

**Consequence:** even with the browser connected, a large share of Phase 3 "passes" would mean *"rendered an empty state without crashing"* — not *"works."* Seed remediation should precede or accompany Phase 3.

---

## 5. Feature inventory (Phase 2) — 587 items, all status `NOT EXECUTED`

Uniform status: **NOT EXECUTED — Phase 3 blocked.** No item below has been runtime-verified.

### customer-web (port 3000) — 246 items

Spec: `docs/SRS.md` §11. `docs/API.md` covers **only** auth/profile/address.

| Route | Auth | Items |
|---|---|---|
| global chrome (header/nav/drawer) | public | 1–10 |
| `/` home | public | 11–16 |
| `/categories` | public | 17–20 |
| `/categories/[slug]` | public | 21–24 |
| `/search` | public | 25–29 |
| `/services/[slug]` | public | 30–40 |
| `/login` | public | 41–52 |
| `/register` | public | 53–59 |
| `/forgot-password` | public | 60–63 |
| `/booking/summary` | login | 64–86 |
| `/booking/payment/[id]` | login | 87–95 |
| `/booking/success/[id]` | login | 96–101 |
| `/bookings` | login | 102–106 |
| `/bookings/[id]` | login | 107–124 |
| `/bookings/[id]/cancel` | login | 125–133 |
| `/bookings/[id]/reschedule` | login | 134–143 |
| `/bookings/[id]/review` | login | 144–152 |
| `/bookings/[id]/track` | login | 153–162 |
| `/addresses` | login | 163–169 |
| `/addresses/new`, `/[id]/edit` | login | 170–175 |
| `/profile` | login | 176–181 |
| `/wallet` | login | 182–185 |
| `/subscription` | login | 186–192 |
| `/refer-earn` | login | 193–201 |
| `/recurring-bookings` | login | 202–209 |
| `/recurring-bookings/new` | login | 210–221 |
| `/support` | login | 222–225 |
| `/support/new` | login | 226–233 |
| `/support/[id]` | login | 234–242 |
| error/404 surfaces | public | 243–246 |

High-risk items requiring explicit attention: double-submit guards on booking create (86), payment (95), cancel (129), review (149), recurring (219); coupon auto-drop when the cart stops qualifying (79); stale-slot revalidation (82); booking draft persistence across navigation (85); open-redirect guard on `?next=` (52).

### admin-web (port 3001) — 227 items

Spec: `docs/SRS.md` §12. `docs/API.md` documents **no** admin endpoints. `dev-admin@nestly.local` is seeded Super Admin → reaches every route.

| Route | Items |
|---|---|
| global chrome | 1–7 |
| `/` redirector, `/login`, `/auth/callback` | 8–13 |
| `/dashboard` | 14–23 |
| `/customers`, `/customers/[id]` | 24–40 |
| `/catalog` (categories) | 41–45 |
| `/catalog/categories/[id]` | 46–48 |
| `/catalog/services`, `/[id]` | 49–58 |
| `/catalog/addons`, `/[id]` | 59–64 |
| `/pricing` | 65–72 |
| `/serviceability` | 73–81 |
| `/serviceability/mappings` | 82–86 |
| `/slots` | 87–96 |
| `/bookings`, `/bookings/[id]` | 97–109 |
| `/coupons`, `/coupons/redemptions` | 110–120 |
| `/support`, `/support/[id]` | 121–130 |
| `/chat`, `/chat/[threadId]` | 131–139 |
| `/reviews` | 140–146 |
| `/cms`, `/cms/banners`, `/cms/faqs` | 147–156 |
| `/notifications` | 157–163 |
| `/reports` | 164–172 |
| `/audit` | 173–175 |
| `/settings` | 176–183 |
| `/admin-users`, `/[id]` | 184–194 |
| `/providers`, `/[id]` | 195–207 |
| `/referral`, `/[id]`, `/config`, `/reports` | 208–221 |
| `/nestly-coins` | 222–224 |
| `/subscription-plans` | 225–227 |

All 21 sidebar hrefs were verified against `app/**/page.tsx` — **no dead nav links**.

### provider-web (port 3002) — 114 items

Spec: `docs/PROVIDER.md`. **`docs/SRS.md` explicitly excludes this app** (§2.3, §4.2) — it contains zero provider-UI requirements.

| Route | Auth | Items |
|---|---|---|
| `/` redirector | public | 1 |
| `/login` | public | 2–10 |
| `/register` | public | 11–19 |
| `/auth/callback` | public | 20–21 |
| app shell (rail, tabs, account menu, guard) | login | 22–27 |
| `/jobs` | login | 28–37 |
| `/jobs/[id]` | login | 38–58 |
| `/availability` | login | 59–75 |
| `/earnings` | login | 76–84 |
| `/earnings/payouts/[id]` | login | 85–88 |
| `/profile` | login | 89–112 |
| 404 / error boundaries | public | 113–114 |

**Tracking-dependent items** (now unblocked by F2, previously untestable): en-route (44), arrived (45), the status-conditional Start choreography (46), location-sharing card, SignalR `/hubs/tracking` subscription.

---

## 6. Documented-but-not-built

Requirements present in the specs with no corresponding UI. **Not defects** — scope gaps that must not be mistaken for regressions.

### customer-web (17)
Footer with static links (SRS 11.1.2) · featured services/promotions on home (11.1.2) · FAQs/testimonials on home (11.1.2) · admin-configurable banners (11.1.3 — `HeroBanner` is hard-coded) · marketing opt-in on register (11.2.1) · mobile+password login mode (11.2.2) · service-card duration/discount/rating (11.5.3) · service-detail duration and terms (11.6.1) · customer notes field anywhere in booking (11.6.2/11.12.2) · **wallet-credit line and apply control at checkout (11.7.2) — yet `/wallet` claims the balance is "applied automatically at checkout"** · wallet/COD payment modes (11.11.1) · payment status + address summary on booking rows (11.13.1) · wallet booking references and expiry (11.17.1) · support attachment + contact preference (11.18.2) · earliest-slot suggestion (11.8.2) · **`referralCode` on register (REFERRAL.md) — a referred customer cannot be attributed at sign-up, the single biggest hole in the referral loop** · public coins-program messaging (NESTLY-COINS.md).

### admin-web (22)
Optional MFA (12.1.1) · **no unlock control despite lockout state being displayed** (12.1.1) · role CRUD screen for the 9 named roles (12.2.2) · permission-matrix editor (12.2.3) · 4 of 9 dashboard KPI widgets (12.3.1) · booking/payment status dashboard filters (12.3.2) · customer registration-date and booking-count filters (12.4.1) · customer profile edit + tagging (12.4.3) · category-level FAQs (12.5.2) · cancellation/reschedule fees as pricing rules (12.8.1) · service↔locality mapping (12.9.2) · category/service scoping on slot windows (12.10.1) · booking filters for category/payment/pincode/source (12.11.1) · booking-detail support tickets, internal notes, audit summary (12.11.2) · re-send confirmation, add note, mark escalation (12.11.3) · coupon name + city/service applicability (12.12.1) · **entire Payment Transaction View (12.13.1)** · ticket-ID filter (12.14.1) · CMS promotional blocks and footer links (12.16.1) · **media upload — banners accept URL only, no `<input type="file">` anywhere in the app** (12.16.2) · cancellation/category/city/review reports (12.18.1) · communication provider settings + public contact details (12.19).

### provider-web (8)
Provider address (PROVIDER.md Identity) · **`provider_capacity` — the auto-assignment engine hard-enforces limits a provider can neither see nor set, and nothing in the codebase can create the row** · rating/performance view (Reputation & Ops; `tasks.csv` 293 still `todo`) · session refresh (dead code, S4) · **provider push notifications — provider-api ships a `DeviceTokensController` (task 277 `done`) but provider-web has no registration, no permission prompt, no UI** · KYC file upload (no storage backend) · provider↔customer chat (exists in the other two apps) · **approved-provider gating — a `PendingVerification` provider can open every screen; `Suspended` gets only a passive alert**.

Confirmed *deliberate*, not gaps: no map/ETA on provider-web (`TRACKING.md:281`); no provider-initiated payout/withdraw (PROVIDER.md decision 3).

---

## 7. Built-but-undocumented

Shipped surface absent from `docs/SRS.md`. Documentation debt and a QA blind spot — nobody writing tests from the SRS would cover any of this.

**Whole modules missing from the SRS:** Providers (admin) · Referral (admin + customer) · Nestly Coins · Subscription Plans / Nestly Plus · Chat (both apps) · Recurring Bookings · Live Tracking · Completion Proof · provider assignment UI on booking detail.

**Cross-cutting:** unified tri-app login with URL-fragment session handoff (in `ARCHITECTURE.md` only) · theme toggle (all 3 apps) · admin table-density preference · booking idempotency keys · client-side draft persistence · `?next=` return-to and its open-redirect validation · HTTP 501 "not yet available" as a distinct third UI state · coupon "Watch" abuse heuristic (redemptions/customer > 1.5, a threshold invented in code) · provider client-side location throttle policy (20s/60s/15m/backoff — `TRACKING.md` documents only the server throttle).

**`docs/API.md` documents only auth/profile/address for customer-web and nothing at all for admin-web.** Every other endpoint across all three apps is undocumented.

---

## 8. Dead / stub UI — will read as bugs to a tester

**Not defects.** Flagged so Phase 3 does not file them.

- customer: availability slot chips are non-interactive `<span>`s (`ServiceAvailability.tsx:151-161`) · masked provider phone is not a `tel:` link (`track/page.tsx:257-278`) · disabled date chips carry their reason only in `title` (touch-inaccessible) · `HeroBanner`/`TrustMarkers` fully static · `Segmented` radios are `sr-only` — **automated clicks must target the visible `<label>`, not the input**.
- admin: `ConfirmDialog` intentionally inerts Escape/backdrop while an action is in flight · SMS channel replaces the Subject field with a **hidden input** · raw GUID text filters on support/reviews/audit.
- provider: `Segmented` ported from customer-web but unused · legacy "Completion proof reference" card duplicates the newer verification card · "Mark complete" permanently disabled if `getCompletionVerification` 404s · `OnboardingProgress` steps look tappable but are inert · **`JobStatus` mirrors backend enum ordinals by hand — any backend reorder silently mislabels every badge**.

---

## 9. Known gaps — explicitly NOT covered

1. **All 587 features are runtime-unverified.** Phase 3 never executed. Nothing in this report constitutes an observed pass.
2. **No cross-service consistency check.** Phase 4 never ran. API contract drift between the three backends is unmeasured — a booking created in customer-web has not been confirmed to appear correctly in admin-web or provider-web.
3. **No client-side bug class covered at all** — CORS in-browser, localStorage, cookies, hydration, console errors, failed network requests, stale-cache behavior on reload-without-clearing. Static analysis cannot reach any of these, and this is precisely the class the brief was designed around.
4. **No bad-input/validation path exercised.** Every form's error state is inventoried but untested.
5. **No empty-state or pagination-boundary verification.**
6. **Seed data cannot support a realistic walkthrough** even once unblocked (§4). A large share of results would be empty-state renders.
7. **Static findings S1–S11 are unconfirmed.** Read from code, not reproduced.
8. **Google Maps unkeyed** — tracking map paths cannot be exercised in this environment regardless of browser connectivity.
9. **No performance, accessibility, security, or mobile-viewport testing** — outside the commissioned scope.
10. **Backend test suites not run** (`Catalog.Tests`, `Identity.Tests`, `CustomerManagement.Tests`, `Performance.Tests`) — outside scope.

---

## 10. Recommendation

**NO-GO for release.** The blocking reason is absence of evidence, not evidence of defects.

To reach a GO:

1. **Connect the Chrome extension** with localhost permission — *user action, hard blocker, nothing proceeds without it.*
2. **Remediate seed data** (§4) — otherwise Phase 3 measures empty states. Minimum: spread bookings across customers/providers/services, add a second real city+zone+pincode, populate `chat_message`, add 1★/2★ reviews, make `EXPIRED10` genuinely expired, backfill the NULL `service_id`/`pincode_id` mapping columns, and fix pincode `560001`'s parent city.
3. **Run Phase 3** — 3 parallel subagents, one per app, executing all 587 items.
4. **Run Phase 4** — cross-service booking consistency.
5. **Confirm or dismiss S1–S11**, and fix S1/S2/S3/S4/S5/S6/S7 before ship.
6. **Decide on the documented-but-not-built set** (§6) — 47 items. These are product scope calls, not engineering defects, but shipping against an SRS that describes them as delivered is a release-notes problem.

The environment is now correct and the database is unblocked. Everything downstream waits on the browser extension.
